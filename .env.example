# MoltX Skills Pack Environment Variables
# NEVER commit this file with real values to git!

# ── 钱包 ──────────────────────────────────────────────────────────────────────
# 私钥由 runtime 自动管理，存放在 ~/.moltx/wallet.json。
# 首次运行自动生成，无需手动配置任何环境变量。
# 请备份 ~/.moltx/wallet.json，丢失私钥意味着丢失资产。

# ── 可选配置 ──────────────────────────────────────────────────────────────────
# 覆盖默认 RPC（默认已内置 Base mainnet）
# MOLTX_RPC_URL=https://base.drpc.org

# 注入预先获取的 API JWT（适合 CI / 短生命周期场景，优先于 ~/.moltx/auth.json）
# MOLTX_API_JWT=your-siwe-jwt
